#include <eseed/gpu/rendercontext.hpp>

#include <eseed/logging/logger.hpp>

using namespace esd::gpu;
using namespace esd::logging;
using namespace esd::window;

RenderContext::RenderContext(std::shared_ptr<Window> window) {
    
}